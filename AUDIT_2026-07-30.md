# Talib Club — รายงานตรวจสอบบัคทั้งเว็บ

**วันที่:** 30 กรกฎาคม 2569 · **Branch:** `main` @ `cdd88c1` · **ขอบเขต:** `src/` (42,651 บรรทัด), `api/`, `firestore.rules`, `storage.rules`, build config

**วิธีตรวจ:** ESLint (313 findings), production build, รัน dev server จริงแล้ววัด layout/a11y ด้วยสคริปต์ที่ viewport 375px / 1280px / 1920px, และอ่านโค้ดแกนกลาง (auth, contentStore, ProNotebook, Quran, api ทั้ง 13 endpoint)

---

## สรุปผู้บริหาร

| ระดับ | จำนวน | สาระสำคัญ |
|---|---|---|
| 🔴 Critical | 4 | ไฟล์ส่วนตัวทุกไฟล์ใน Storage เปิดสาธารณะ · สมุดโน้ตถูกเขียนทับเป็นหน้าเปล่า · AI proxy เปิดให้ใครก็ยิงได้ · SSRF |
| 🟠 High | 8 | PII รั่วผ่าน Firestore rules · ปุ่ม 6 ตัวใน notebook throw · autosave ไม่ทำงานตอนใช้จริง · การ์ดทุกใบกด keyboard ไม่ได้ |
| 🟡 Medium | 17 | logic/consistency/UX/perf |
| ⚪ Low | 10 | code hygiene |

**3 อย่างที่ควรแก้ก่อนสิ้นวันนี้:** C1 (storage.rules), C2 (notebook data loss), C3 (`api/ai.js`)

---

## ✅ สถานะการแก้ไข (อัปเดต 30 ก.ค. 2569 — แก้จากยากไปง่าย)

**แก้แล้ว 27 รายการ** ใน 26 ไฟล์ (+1 ไฟล์ใหม่ `api/track-lookup.js`)

| กลุ่ม | รายการที่แก้ | ไฟล์หลัก |
|---|---|---|
| Notebook data-safety | C2, H5, M15, M16, M1 | `notebookStorage.js`, `ProNotebook.jsx`, `recovery.js` |
| PII leak | H1, H2 | `api/track-lookup.js` (ใหม่), `Tracking.jsx`, `firestore.rules` |
| A11y / UX | H8, M9, M10, M11, M17 | `ui/index.js`, `ArticleCard.jsx`, `Nav.jsx`, `Home/Media/Library/Scholars.jsx`, `global.css`, `utils/ui.js` |
| API security | C3, C4, M13, M14 | `api/ai.js`, `api/proxy-pdf.js`, `api/image-search.js`, `api/generate-quiz.js` |
| Storage rules | C1 | `storage.rules` |
| Auth gate | H6 | `App.jsx`, `Nav.jsx` |
| Data layer | M3, M4, M5, M6, M8 | `contentStore/hooks.js`, `App.jsx` |
| Logic เล็ก | H3, H4, M2, M12, Low9, Low10 | `TextEditor.jsx`, `ProNotebook.jsx`, `Nav.jsx`, `ReflectionsPanel.jsx`, `eslint.config.js` |

**ผลตรวจหลังแก้ (วัดใน dev server จริง):**
- ESLint: **120 → 103 errors**, `no-undef` **21 → 0**
- การ์ดที่กดได้ทั้งหมด focus ด้วยคีย์บอร์ดได้: หน้า Home **10/10**, /articles **6/6** (ก่อนแก้ 0)
- ปุ่ม/ลิงก์ที่ไม่มีชื่อ: **6 → 0**
- hamburger **20×30 → 44×44**, theme toggle **34×34 → 44×44**, `ดูทั้งหมด` **62×18 → 86×44**
- จอ 1920px: `main` **1100 → 1440px**, grid **3 → 4 คอลัมน์**
- ไม่มี horizontal overflow, ไม่มี console error, `npm run build` ผ่าน

**ยังไม่ได้แก้ (เหลือไว้เจตนา):**
- **H7** entry chunk 926 kB — ต้องรื้อ `manualChunks` + แยก dynamic import ของ firebase/storage เสี่ยงกระทบวงกว้าง ควรทำแยก PR พร้อมทดสอบ
- **M7** `vite.config.js` ไม่ถูกใช้ — ต้องตัดสินใจก่อนว่าจะลบไฟล์ไหน
- **M11 บางส่วน** — `SecHeader` render `<h2>` แล้ว แต่หน้า Home/Scholars ใช้ header แบบ inline (`.sec-link`) จึงยังได้ `h2: 0` ต้องแก้ทีละหน้า
- **Low 1–8** — code hygiene (dead code, `FormatBtn` in render, `addDaysToKey` DST, title ซ้ำ, `rel=noopener`)

⚠️ **ต้อง deploy rules แยก** — การแก้ `storage.rules` และ `firestore.rules` ไม่ไปกับ Vercel:
```bash
firebase deploy --only firestore:rules,storage
```
และ **ยืนยันด้วย Rules Playground** ก่อน โดยเฉพาะ `storage.rules` ที่เปลี่ยนจาก catch-all เป็น prefix ที่ระบุชัด (ถ้ามีรูปเก่าอยู่ prefix อื่นนอกจาก `article_covers/`, `library_covers/`, `campaign_images/` ต้องเพิ่มเข้าไป)

---

# 🔴 CRITICAL

## C1 — `storage.rules`: กฎ catch-all ทำให้ไฟล์ส่วนตัวทุกไฟล์เปิดสาธารณะ

**ไฟล์:** `storage.rules:20-38`

Firebase Security Rules ใช้ **OR semantics** — ถ้ามี `match` block ใดก็ตามที่ตรง path และ `allow` เป็น true ระบบจะอนุญาต **ทันที** โดยไม่สนใจว่า block อื่นจะเข้มกว่า

```
match /notebooks/{userId}/{allPaths=**} {
  allow read, write: if request.auth != null && request.auth.uid == userId;   // ← ตั้งใจให้ส่วนตัว
}

match /{allPaths=**} {
  allow read: if true;          // ← แต่ block นี้ก็ match path เดียวกัน และปล่อยผ่านหมด
  allow write: if ...staff...
}
```

`notebooks/abc123/xyz.json.gz` ตรงกับ **ทั้งสอง** block → `allow read: if true` ชนะ

**ผลกระทบ:**
- 📓 สมุดโน้ตของสมาชิกทุกคน อ่านได้โดยไม่ต้อง login
- 🧾 **สลิปโอนเงิน** ใน `/slips/` อ่านได้โดยไม่ต้อง login — กฎ private ที่เขียนไว้ใน `slips/{fileName}` ตายสนิท
- ใน Storage rules `read` = `get` + **`list`** → คนนอกสามารถ **list ทั้ง bucket** เพื่อไล่เก็บชื่อไฟล์ทั้งหมดได้

**แนวทางแก้ —** ย้ายไฟล์สาธารณะไปอยู่ใต้ prefix ของตัวเอง แล้วให้ catch-all แคบลง:

```
rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {

    match /slips/{fileName} {
      allow read: if request.auth != null && (
        fileName.matches(request.auth.uid + '.*') ||
        firestore.get(/databases/(default)/documents/users/$(request.auth.uid)).data.role in ["staff","admin","owner"]
      );
      allow create: if request.auth != null
                    && fileName.matches(request.auth.uid + '.*')
                    && request.resource.size < 5 * 1024 * 1024
                    && request.resource.contentType.matches('image/.*');
    }

    match /notebooks/{userId}/{allPaths=**} {
      allow read, write: if request.auth != null && request.auth.uid == userId;
    }

    // เฉพาะ prefix ที่เป็น asset สาธารณะจริง ๆ — ไม่ใช้ {allPaths=**} ที่ root
    match /covers/{allPaths=**}  { allow read: if true; allow write: if isStaff(); }
    match /articles/{allPaths=**} { allow read: if true; allow write: if isStaff(); }
    match /media/{allPaths=**}   { allow read: if true; allow write: if isStaff(); }

    function isStaff() {
      return request.auth != null &&
        firestore.get(/databases/(default)/documents/users/$(request.auth.uid)).data.role
          in ["staff","admin","owner"];
    }
  }
}
```

⚠️ ต้องสำรวจก่อนว่าตอนนี้อัปโหลดรูปไปไว้ prefix ไหนบ้าง (`AdminArticles`, `AdminLibrary`, `AdminMedia`, `AdminScholars`) แล้วใส่ให้ครบ ไม่งั้นรูปหน้าเว็บจะหาย — และยืนยันผลด้วย **Rules Playground** ใน Firebase Console ก่อน deploy

---

## C2 — สมุดโน้ตถูกเขียนทับเป็นหน้าเปล่าเมื่อโหลดจาก cloud ไม่สำเร็จ

**ไฟล์:** `src/utils/notebookStorage.js:41-71` + `src/pages/reading/components/ProNotebook.jsx:81-102, 752-761`

ต้นตอ — `downloadNotebookData` กลืน error ทุกชนิดแล้ว return `null`:

```js
} catch (err) {
  // Return null if not found (e.g. new notebook)
  return null;              // ← network fail / permission / decompress fail ก็ได้ null เหมือนกัน
}
```

ทำให้ `catch` ฝั่งผู้เรียก **ไม่มีทางทำงาน** — fallback localStorage เป็น dead code:

```js
try {
   const cloudData = await downloadNotebookData(uid, notebookId, ...);
   if (cloudData && cloudData.length > 0) { setPages(cloudData); ... }
} catch (e) {                                    // ← ไม่เคยเข้า เพราะไม่มีอะไร throw
   const saved = localStorage.getItem(`talib_notebook_${notebookId}`);
   if (saved) setPages(JSON.parse(saved));
}
```

**ลำดับความเสียหาย:**
1. เน็ตกระตุก / Storage ติด rate-limit / เบราว์เซอร์ไม่รองรับ `DecompressionStream` → ได้ `null`
2. ไม่มี toast ไม่มี error — `pages` คงค่า default คือ **หน้าเปล่า 1 หน้า**
3. fallback localStorage ถูกข้าม
4. effect autosave (`deps: [pages, readonly]`) ทำงานตั้งแต่ mount — `pages.length === 1` ผ่าน guard `pages.length === 0`
5. ครบ 5 วินาที → `saveNotebook(true)` → `uploadNotebookData(uid, notebookId, pages)`
6. **สมุดโน้ตบน cloud ถูกทับด้วยหน้าเปล่า และ backup ใน localStorage ก็ถูกทับด้วย**

งานทั้งเล่มหายถาวรจากเน็ตกระตุกครั้งเดียว โดยผู้ใช้ไม่ได้รับการเตือนอะไรเลย

**แนวทางแก้ — 3 ชั้น (ควรทำทั้งสามชั้น):**

**ชั้น 1 · แยก "ไม่มีไฟล์" ออกจาก "โหลดพลาด"** — `notebookStorage.js`:

```js
export async function downloadNotebookData(uid, notebookId, onProgress) {
  const storageRef = ref(storage, `notebooks/${uid}/${notebookId}.json.gz`);
  let url;
  try {
    url = await getDownloadURL(storageRef);
  } catch (err) {
    // ไฟล์ไม่มีจริง = สมุดใหม่ → null เป็นคำตอบที่ถูก
    if (err?.code === 'storage/object-not-found') return null;
    throw err;                                   // อย่างอื่นคือ "พลาด" → ต้อง throw
  }
  const response = await fetch(url);
  if (!response.ok) throw new Error(`Notebook fetch failed: ${response.status}`);
  // ... (โค้ดอ่าน stream เดิม) ...
  return JSON.parse(await decompressBlob(blob));   // ให้ throw ถ้า parse/decompress พลาด
}
```

**ชั้น 2 · ห้าม autosave ก่อนที่การโหลดจะจบเรียบร้อย** — `ProNotebook.jsx`:

```js
const loadStateRef = useRef('loading');   // 'loading' | 'ready' | 'failed'

useEffect(() => {
  const loadData = async () => {
    setIsSyncing(true);
    try {
      const cloudData = await downloadNotebookData(uid, notebookId, setSyncProgress);
      if (cloudData?.length > 0) {
        setPages(cloudData);
        toast.success("ซิงก์ข้อมูลสำเร็จ!", { id: "cloud-sync" });
      }
      loadStateRef.current = 'ready';          // สมุดใหม่ก็ ready — เซฟได้
    } catch (e) {
      console.error("Cloud load failed", e);
      const saved = localStorage.getItem(`talib_notebook_${notebookId}`);
      if (saved) {
        setPages(JSON.parse(saved));
        loadStateRef.current = 'ready';
        toast.error("ออฟไลน์: โหลดจากเครื่องแทน", { id: "cloud-sync" });
      } else {
        loadStateRef.current = 'failed';      // ← ไม่รู้ว่าของเดิมมีอะไร ห้ามเซฟทับ
        toast.error("โหลดสมุดโน้ตไม่สำเร็จ — ระบบปิดการบันทึกอัตโนมัติไว้เพื่อป้องกันข้อมูลเดิมหาย", 
                    { id: "cloud-sync", duration: 10000 });
      }
    } finally {
      setIsSyncing(false); setSyncProgress(null);
    }
  };
  if (uid && notebookId !== 'default') loadData();
  else { /* โหลด local เดิม */ loadStateRef.current = 'ready'; }
}, [uid, notebookId]);
```

**ชั้น 3 · ใส่ guard ที่ประตูเขียนทุกบาน:**

```js
const saveNotebook = async (isAuto = false) => {
  if (readonly) return;
  if (loadStateRef.current !== 'ready') {
    if (!isAuto) toast.error("ยังโหลดสมุดโน้ตไม่สำเร็จ — บันทึกไม่ได้เพื่อป้องกันข้อมูลเดิมหาย");
    return;
  }
  ...
};
```

และใน cleanup ตอน unmount (`ProNotebook.jsx:763-771`) เพิ่ม `if (loadStateRef.current !== 'ready') return;` ด้วย

---

## C3 — `api/ai.js` เป็น AI proxy ที่ใครก็ยิงได้ ไม่มี auth

**ไฟล์:** `api/ai.js`

```js
'Access-Control-Allow-Origin': '*',
...
const payload = await req.json();
if (!payload.model) payload.model = 'openai/gpt-4o-mini';
headers.set('Authorization', `Bearer ${key}`);      // ← คีย์ที่คุณจ่ายเงิน
const upstream = await fetch(BASE, { method:'POST', headers, body: JSON.stringify(payload) });
```

ไม่มี auth · ไม่เช็ค origin · ไม่มี rate limit · ไม่มี model allowlist · ส่ง payload ทั้งก้อนจาก client ต่อขึ้น OpenRouter **รวมถึงฟิลด์ `model`**

ใครก็ตามบนอินเทอร์เน็ตยิงได้:
```
POST https://<โดเมนคุณ>/api/ai
{"model":"<โมเดลแพงสุดที่มี>","messages":[...],"max_tokens":100000}
```
→ บิล OpenRouter ของคุณ ไม่มีเพดาน เทียบกับ `api/send-telegram.js` และ `api/generate-quiz.js` ในโปรเจกต์เดียวกันที่ทำ auth ไว้ถูกต้องแล้ว ไฟล์นี้แค่หลุด

**แนวทางแก้:**

```js
// api/ai.js — ต้องเปลี่ยนจาก edge runtime เป็น node เพราะ firebase-admin
// export const config = { runtime: 'edge' };   ← ลบออก
import { verifyIdToken } from './_firebase-admin.js';

const ALLOWED_ORIGIN = process.env.ALLOWED_ORIGIN || 'https://talibclub.org';
const ALLOWED_MODELS = new Set(['openai/gpt-4o-mini', 'anthropic/claude-3.5-haiku']);
const MAX_TOKENS = 2048;

export default async function handler(req, res) {
  res.setHeader('Access-Control-Allow-Origin', ALLOWED_ORIGIN);      // ไม่ใช่ '*'
  res.setHeader('Access-Control-Allow-Methods', 'POST, OPTIONS');
  res.setHeader('Access-Control-Allow-Headers', 'Content-Type, Authorization');
  if (req.method === 'OPTIONS') return res.status(204).end();
  if (req.method !== 'POST') return res.status(405).json({ error: 'method_not_allowed' });

  const auth = req.headers.authorization || '';
  if (!auth.startsWith('Bearer ')) return res.status(401).json({ error: 'unauthorized' });
  let uid;
  try { uid = (await verifyIdToken(auth.slice(7))).uid; }
  catch { return res.status(401).json({ error: 'unauthorized' }); }

  if (!(await checkRateLimit(uid))) return res.status(429).json({ error: 'rate_limited' });

  const body = req.body || {};
  // สร้าง payload ใหม่จาก allowlist — ไม่ forward ของ client ทั้งก้อน
  const payload = {
    model: ALLOWED_MODELS.has(body.model) ? body.model : 'openai/gpt-4o-mini',
    messages: Array.isArray(body.messages) ? body.messages.slice(-20) : [],
    max_tokens: Math.min(Number(body.max_tokens) || 1024, MAX_TOKENS),
    temperature: Math.min(Math.max(Number(body.temperature) || 0.2, 0), 1),
    stream: Boolean(body.stream),
  };
  // ... fetch upstream ...
}
```

`checkRateLimit` ทำง่าย ๆ ด้วย Firestore: doc `ai_usage/{uid}` เก็บ `{ dayKey, count }` แล้วจำกัดเช่น 50 ครั้ง/วัน/คน ผ่าน transaction

---

## C4 — `api/proxy-pdf.js` เป็น SSRF + open proxy

**ไฟล์:** `api/proxy-pdf.js:8, 27, 56-70`

```js
const targetUrl = url.searchParams.get('url');
let response = await fetch(targetUrl);            // ← ไม่ validate อะไรเลย
...
const headers = new Headers(response.headers);    // ← copy header ต้นทางมาทั้งหมด
headers.set('Access-Control-Allow-Origin', '*');
return new Response(response.body, { status: 200, headers });
```

ไม่มี auth · ไม่มี allowlist host · ไม่เช็ค scheme · **ไม่เช็ค content-type** (ชื่อ proxy-pdf แต่ส่งต่ออะไรก็ได้) · copy header ต้นทาง (รวม `Set-Cookie`) · บังคับ `status: 200` แม้ต้นทางจะตอบอย่างอื่น

ใช้เป็น: probe เครือข่ายภายใน (`?url=http://localhost:8080/`), metadata endpoint, และ **bandwidth proxy นิรนามสำหรับเนื้อหาอะไรก็ได้** โดยทราฟฟิกและความรับผิดไปลงที่โดเมนคุณ

**แนวทางแก้:**

```js
const ALLOWED_HOSTS = new Set([
  'drive.google.com',
  'docs.google.com',
  'firebasestorage.googleapis.com',
]);

export default async function handler(req) {
  const cors = { 'Access-Control-Allow-Origin': process.env.ALLOWED_ORIGIN || 'https://talibclub.org' };
  if (req.method === 'OPTIONS') return new Response(null, { status: 204, headers: cors });

  const raw = new URL(req.url).searchParams.get('url');
  if (!raw) return new Response('Missing url', { status: 400, headers: cors });

  let target;
  try { target = new URL(raw); } catch { return new Response('Bad url', { status: 400, headers: cors }); }
  if (target.protocol !== 'https:')       return new Response('https only', { status: 400, headers: cors });
  if (!ALLOWED_HOSTS.has(target.hostname)) return new Response('Host not allowed', { status: 403, headers: cors });

  const upstream = await fetch(target.toString(), { redirect: 'follow' });
  // ... (logic bypass virus-scan ของ Google Drive เดิม — ตรวจ host อีกครั้งหลัง redirect) ...
  if (!upstream.ok) return new Response('Upstream error', { status: upstream.status, headers: cors });

  const ct = upstream.headers.get('content-type') || '';
  if (!/^application\/(pdf|octet-stream)/i.test(ct)) {
    return new Response('Not a PDF', { status: 415, headers: cors });
  }

  // สร้าง header ใหม่ ไม่ copy ของต้นทาง
  return new Response(upstream.body, {
    status: 200,
    headers: { ...cors, 'Content-Type': 'application/pdf',
               'Content-Disposition': 'inline; filename="document.pdf"' },
  });
}
```

พิจารณาเพิ่ม `verifyIdToken` ด้วย — `/library-detail` และ `/reader` อยู่หลัง `RequireLogin` อยู่แล้ว

---

# 🟠 HIGH

## H1 — Firestore rules: สมาชิกทั่วไปอ่านข้อมูลผู้รับพัสดุของคนอื่นได้

**ไฟล์:** `firestore.rules:169-179`

```
match /recipients/{docId} {
  allow list: if isStaff() || (signedIn() && request.query.limit != null && request.query.limit <= 5);
}
```

เจตนาคือให้หน้า tracking ค้นหาแบบ exact match ได้ (`Tracking.jsx:164-170` ใช้ `where fullName == / phone ==` + `limit(5)`) แต่ **Firestore rules ไม่สามารถบังคับ `where` clause ได้** — เช็คได้แค่ `limit`

สมาชิกที่ login แล้วรันได้เลย:
```js
const q = query(collection(db, 'recipients'), limit(5));   // ไม่มี where
const snap = await getDocs(q);   // ← ได้ชื่อ-นามสกุล เบอร์โทร ที่อยู่ ของคนอื่น 5 ราย
```
แล้วเปลี่ยน `orderBy` เป็นฟิลด์ต่าง ๆ / ใช้ `startAfter` ไล่เก็บทั้ง collection ได้ `records` มีกฎเดียวกันเป๊ะ

**แนวทางแก้** — ย้าย lookup ขึ้น server แล้วปิดสิทธิ์ list ฝั่ง client:

```
match /recipients/{docId} { allow get, list, write: if isStaff(); }
match /records/{docId}    { allow get, list, write: if isStaff(); }
```

สร้าง `api/track-lookup.js` (ใช้ firebase-admin, ต้องมี ID token) ที่รับ `fullName`/`phone` แล้วคืน **เฉพาะ record ที่ตรง** พร้อมตัดฟิลด์ที่ไม่จำเป็นออก (ไม่ต้องคืนที่อยู่เต็มถ้าแค่จะบอกสถานะพัสดุ) และใส่ rate limit กัน brute-force เบอร์โทร

## H2 — `site_visits` เปิดให้เขียนโดยไม่ต้อง login

**ไฟล์:** `firestore.rules:52-55` — `allow create: if true;`

ใครก็ยิง `addDoc(collection(db,'site_visits'), {...})` ได้ไม่จำกัด → ค่า Firestore write บวม + สถิติเพี้ยน

**แก้:** `allow create: if signedIn() && request.resource.data.keys().hasOnly(['createdAt','userAgent','path','uid','displayName','email']);` — หรือถ้าต้องนับ guest จริง ให้ย้ายไปเป็น API endpoint ที่มี rate limit ต่อ IP

## H3 — `toggleFormat` is not defined → ปุ่ม 5 ตัวใน TextEditor throw

**ไฟล์:** `src/pages/reading/components/notebook/TextEditor.jsx:329, 330, 336, 337, 338`

ปุ่ม bullet list, numbered list, align left/center/right ทั้งหมดเรียก `toggleFormat(...)` ซึ่ง**ไม่มีอยู่ในไฟล์** → กดแล้ว `ReferenceError` ไม่มีอะไรเกิดขึ้น (error handler ของ React ไม่จับ error ใน event handler จึงไม่ขึ้น error card — เงียบสนิท)

ฟังก์ชันจริงชื่ออื่น และ ESLint รายงานว่า**ไม่ถูกใช้งาน** (บรรทัด 210-211):
```js
const setAlign   = (val) => applyToLines((f) => ({ ...f, align: val }));
const toggleList = (val) => applyToLines((f, all) => ({ ...f, list: all.every(s => s.list === val) ? 'none' : val }));
```

เป็น regression จาก commit `3b539f0` ("restore missing FormatBtn component")

**แก้:**
```js
// บรรทัด 329-330
<FormatBtn icon={<List size={15} />}        active={active.list === 'bullet'} onClick={() => toggleList('bullet')} />
<FormatBtn icon={<ListOrdered size={15} />} active={active.list === 'number'} onClick={() => toggleList('number')} />
// บรรทัด 336-338
<FormatBtn icon={<AlignLeft size={15} />}   active={!active.align || active.align === 'left'} onClick={() => setAlign('left')} />
<FormatBtn icon={<AlignCenter size={15} />} active={active.align === 'center'}                onClick={() => setAlign('center')} />
<FormatBtn icon={<AlignRight size={15} />}  active={active.align === 'right'}                 onClick={() => setAlign('right')} />
```

## H4 — `exportPage` is not defined → เมนู "บันทึกรูปหน้านี้ (PNG)" throw

**ไฟล์:** `src/pages/reading/components/ProNotebook.jsx:3136`

```js
<button onClick={() => { exportPage(); setShowMoreMenu(false); }}>   // ← ไม่มีฟังก์ชันนี้
```

**แก้** — ใช้ `runExport` ที่มีอยู่แล้ว (`ProNotebook.jsx:1226`):
```js
<button onClick={() => { setShowMoreMenu(false); runExport('png', 'current'); }}>
```

## H5 — Autosave ไม่ทำงานระหว่างใช้งานจริง

**ไฟล์:** `src/pages/reading/components/ProNotebook.jsx:752-761`

```js
useEffect(() => {
  if (readonly || !pages || pages.length === 0) return;
  const timer = setTimeout(() => saveNotebook(true), 5000);
  return () => clearTimeout(timer);
}, [pages, readonly]);
```

debounce reset **ทุกครั้งที่ `pages` เปลี่ยน** และทุกเส้นที่ลากคือการเปลี่ยน `pages` → คนที่เขียนต่อเนื่อง 30 นาที **ไม่มี cloud save เลยแม้แต่ครั้งเดียว** จะเซฟก็ต่อเมื่อหยุดนิ่งครบ 5 วินาที

ทางรอดที่เหลือคือ save ตอน unmount แต่ `window.location.reload()` (ซึ่ง `recovery.js` เรียกได้เอง — ดู M1) **ไม่รัน unmount cleanup ของ React** → หายทั้งหมด

**แก้** — ใส่ max-wait flush คู่กับ idle debounce:

```js
const lastSaveRef = useRef(Date.now());
const MAX_WAIT = 45000;   // เซฟอย่างน้อยทุก 45 วินาทีระหว่างเขียนต่อเนื่อง

useEffect(() => {
  if (readonly || !pages?.length) return;
  const sinceLast = Date.now() - lastSaveRef.current;
  const delay = sinceLast >= MAX_WAIT ? 0 : Math.min(5000, MAX_WAIT - sinceLast);
  const timer = setTimeout(() => {
    lastSaveRef.current = Date.now();
    saveNotebook(true);
  }, delay);
  return () => clearTimeout(timer);
}, [pages, readonly]);
```

เพิ่มด้วย: `beforeunload` handler ที่เขียน localStorage แบบ sync, และกันการเซฟซ้อน (`saveNotebook` ยังไม่มี in-flight guard — manual save กับ autosave ยิงพร้อมกันได้ และ `setTimeout(() => setIsSaving(false), 1500)` ใน `finally` ทำให้ตัวหลังลบสถานะของตัวแรก)

## H6 — หน้าเว็บสาธารณะทั้งหมดรอ Firebase Auth ก่อนแสดงอะไรเลย

**ไฟล์:** `src/App.jsx:227-241` + `src/hooks/useAuth.js:79-89`

```js
if (authState.loading) {
  return (<div style={{ height:"100vh", ... }}>Talib ... กำลังเตรียมความพร้อมของระบบ...</div>)
}
```

return ก่อน `<Routes>` ทั้งหมด → `/`, `/articles`, `/library`, `/media`, `/scholars`, `/donate` ที่เป็น public content ล้วน ๆ ก็ถูกบล็อกด้วย และ `useAuth` มี fallback timeout **6000ms** สำหรับกรณี AdBlocker บล็อก Firebase

ผู้เข้าชมครั้งแรก / bot / คนเน็ตช้า → เห็นสปินเนอร์ได้ถึง 6 วินาทีก่อนเห็นเนื้อหา กระทบทั้ง UX และ Core Web Vitals (LCP) ของเว็บที่เป้าหมายหลักคือการอ่าน

**แก้** — ย้าย gate ลงไปที่ subtree ที่ต้องใช้ auth จริง (`RequireLogin` / `RequireStaff` มี `if (authState.loading) return <LoadingState/>` อยู่แล้ว):

```js
// ลบ early-return ทิ้งทั้งบล็อก แล้วให้ Nav รับมือ loading เอง
return (
  <div className={`app ${theme}`}>
    <Toaster ... />
    <Nav ... authState={authState} />      {/* แสดง skeleton ที่ปุ่ม login ระหว่างรอ */}
    <main ...>
      <Routes>...</Routes>                  {/* public routes render ทันที */}
    </main>
  </div>
)
```

route ที่ต้อง login ยังปลอดภัยเพราะ `RequireLogin` เช็ค `loading` อยู่แล้ว

## H7 — Entry chunk 926 kB โหลดทุกหน้า

จาก production build:
```
dist/assets/index-CTzOJRcA.js       926.11 kB │ gzip: 241.23 kB   ← shared chunk ทุกหน้า
dist/assets/ProNotebook-DRwNTo0W.js 1,032.62 kB │ gzip: 307.73 kB
dist/assets/Admin-BWTkm6Ud.js         772.73 kB │ gzip: 211.21 kB
dist/assets/jspdf.es.min-DSQaeENB.js  390.46 kB │ gzip: 128.79 kB
dist/assets/html2canvas.esm-...js     201.42 kB │ gzip:  48.03 kB
```

`src/lib/firebase.js` import `firebase/auth` + `firebase/firestore` (พร้อม `persistentLocalCache`) + `firebase/storage` แบบ static และถูก import จาก `App.jsx` → ทั้งหมดตกอยู่ใน entry chunk คนที่เข้ามาอ่านบทความเดียวต้องดาวน์โหลด Firestore SDK เต็มก้อน

**แก้:**
1. แยก `firebase/storage` ออกเป็น dynamic import — ใช้แค่ใน notebook/admin upload
2. เลื่อน `persistentLocalCache` ไปเรียกหลัง first paint หรือเฉพาะ authenticated route
3. ใส่ `manualChunks` ใน **`scripts/vite-runner.mjs`** (ไม่ใช่ `vite.config.js` — ดู M7):
```js
build: {
  chunkSizeWarningLimit: 800,
  rollupOptions: { output: { manualChunks: {
    'firebase-core': ['firebase/app', 'firebase/auth'],
    'firebase-data': ['firebase/firestore', 'firebase/storage'],
    'konva': ['konva', 'react-konva'],
    'pdf': ['pdfjs-dist'],
  }}},
}
```
4. `jspdf`/`html2canvas` ควรเป็น dynamic import แล้ว — ตรวจว่า `recharts` (`AdminDashboard`) และ `tesseract.js` ไม่ได้ถูกดึงเข้า chunk ที่โหลดเร็วเกินจำเป็น

## H8 — การ์ดเนื้อหาทุกใบเข้าถึงด้วยคีย์บอร์ดไม่ได้เลย

**วัดจริงบนหน้า Home (375px):**
```
.card ทั้งหมด            : 14 ใบ
การ์ดที่ focus ได้        : 0
การ์ดที่มี role           : 0
element ที่ focus ได้ทั้งหน้า: 16  (และ 6 ตัวไม่มีชื่อ)
```
**หน้า `/articles`:** element ที่ cursor เป็น pointer แต่ไม่ใช่ `<button>`/`<a>` = **112 ตัว**, focus ได้ทั้งหน้า = **10 ตัว**

ต้นตอ — `src/components/ArticleCard.jsx:6-17` และ `src/components/ui/index.js:102-116` เป็น `<div onClick>` เปล่า ๆ ไม่มี `role`, `tabIndex`, ไม่มี key handler

**ผลกระทบ:** คนที่ใช้คีย์บอร์ด/screen reader เปิดบทความ หนังสือ หรือมีเดียไม่ได้เลยแม้แต่ชิ้นเดียว · คลิกกลางเพื่อเปิดแท็บใหม่ไม่ได้ · คลิกขวา copy link ไม่ได้ · Google ไม่เห็นลิงก์ภายในเพราะไม่มี `<a href>` (กระทบ SEO ของทั้งเว็บ)

**แนวทางแก้ที่ดีที่สุด** — ทำให้เป็นลิงก์จริง ได้ทั้ง a11y, SEO และพฤติกรรมเบราว์เซอร์ปกติครบในทีเดียว:

```jsx
// src/components/ArticleCard.jsx
import { Link } from "react-router-dom"
import { getPagePath } from "../utils/url.js"

export default function ArticleCard({ article: a, onClick, coverHeight = 160 }) {
  return (
    <Link
      to={getPagePath("article", a)}
      state={{ ctx: a }}
      className="card"
      onClick={(e) => { if (onClick && !e.metaKey && !e.ctrlKey) { /* ให้ router จัดการ */ } }}
      style={{ display:"flex", flexDirection:"column", justifyContent:"space-between",
               height:"100%", overflow:"hidden", textDecoration:"none", color:"inherit" }}
    >
      {/* ...เนื้อหาเดิม... */}
    </Link>
  )
}
```

ถ้ายังไม่พร้อมรื้อ ให้ทำ minimum viable fix ที่ `ui/index.js` `Card` ก่อน:

```jsx
export function Card({ children, onClick, style = {}, label }) {
  const interactive = Boolean(onClick)
  return (
    <div
      onClick={onClick}
      role={interactive ? "button" : undefined}
      tabIndex={interactive ? 0 : undefined}
      aria-label={label}
      onKeyDown={interactive ? (e) => {
        if (e.key === "Enter" || e.key === " ") { e.preventDefault(); onClick(e) }
      } : undefined}
      style={{ background:"var(--card)", border:".5px solid var(--br2)", borderRadius:12,
               transition:"border-color .2s", cursor: interactive ? "pointer" : "default", ...style }}
      ...
    >{children}</div>
  )
}
```
และเพิ่ม `:focus-visible` outline ใน `global.css` — ตอนนี้ไม่มี focus style ที่มองเห็นได้เลย

---

# 🟡 MEDIUM

## M1 — `recovery.js` จับ error กว้างเกินไป กลบบัคจริงและทำข้อมูลหาย

**ไฟล์:** `src/utils/recovery.js:16-24`

```js
const STALE_PATTERNS = [
  /before initialization/i,
  /is not defined/i,          // ← บัคธรรมดา
  /is not a function/i,       // ← บัคธรรมดา
  /failed to fetch dynamically imported module/i,
  /error loading dynamically imported module/i,
  /unexpected token/i,        // ← บัคธรรมดา
  /import\(\) failed/i,
];
```

บน production error ธรรมดาใน render phase (เช่น `x.map is not a function`) จะถูกตีความเป็น "bundle เก่า" → ล้าง cache ทั้งหมด + unregister service worker + **`window.location.reload()`** ผลคือ (ก) บัคจริงถูกซ่อน เห็นแค่ "กำลังอัปเดตเป็นเวอร์ชันล่าสุด" (ข) บน `/reader` การ hard reload ข้าม unmount cleanup → งานที่ยังไม่เซฟหาย (โยงกับ H5)

**แก้** — เหลือแต่ pattern ที่บ่งชี้ chunk mismatch จริง:
```js
const STALE_PATTERNS = [
  /failed to fetch dynamically imported module/i,
  /error loading dynamically imported module/i,
  /import\(\) failed/i,
  /before initialization/i,                          // TDZ จาก chunk ไม่เข้าคู่
  /Failed to load module script/i,
];
```

## M2 — ตัวชี้ "อ่านวันนี้แล้ว" ไม่ข้ามวันตอนเที่ยงคืน + ใช้นาฬิกาที่ไม่ sync

**ไฟล์:** `src/components/Nav.jsx:174`

```js
const hasReadToday = useMemo(() => {
  const today = getLocalDayKey(Date.now())      // ← เรียก Date.now() ใน render
  ...
}, [readingSessions, uid])                      // ← deps ไม่มีเวลา
```

สองปัญหา: (1) recompute เฉพาะเมื่อ `readingSessions`/`uid` เปลี่ยน — แท็บที่เปิดคาข้ามเที่ยงคืนยังยึด day key ของเมื่อวาน (2) ใช้ `Date.now()` ขณะที่ทั้งโปรเจกต์ใช้ `safeDateNow()` ที่ปรับ offset กับเวลา server แล้ว (`src/utils/time.js`) — เครื่องที่นาฬิกาเพี้ยนจะได้คำตอบไม่ตรงกับที่ streak คำนวณ

**แก้:** ใช้ `safeDateNow()` และเพิ่ม state วันปัจจุบันที่ tick ทุกนาที ใส่ใน deps

## M3 — `bulkDeleteItems` mark ว่าลบแล้วรวมถึง chunk ที่ล้มเหลว

**ไฟล์:** `src/lib/contentStore/hooks.js:508-520` — วน `ids` ทั้งชุดตอนอัปเดต cache ไม่ใช่เฉพาะตัวที่ commit สำเร็จ → item ที่ batch พังหายจากหน้าจอแต่ยังอยู่ใน Firestore กลับมาโผล่ตอน reload
(`const now = serverTimestamp()` ที่บรรทัด 487 เป็น dead code ด้วย)

**แก้:** เก็บ `succeeded` แล้วใช้ set นั้นอัปเดต cache

## M4 — ลบเนื้อหาแบบ hard delete ชนกับ fallback merge → ข้อมูลที่ลบกลับมา

`deleteItem`/`deleteContentItem` ใช้ `deleteDoc()` แต่ `mergeWithFallback` (`utils.js:78-96`) จะตัด fallback item ออก**เฉพาะเมื่อยังมี remote doc ที่ `deleted: true`** อยู่ ลบ item ที่มีใน `src/data/articles.js` / `books.js` / `media.js` / `scholars.js` ด้วย → cache หายทันที แต่ cold load ครั้งถัดไปมันกลับมาจากไฟล์ fallback

**แก้:** ถ้า id นั้นมีใน fallback ให้ soft delete `setDoc(ref, { deleted:true, updatedAt:... }, { merge:true })` แทน — หรือถ้า Firestore เป็นแหล่งจริงแล้ว ก็เลิกใช้ไฟล์ fallback ไปเลย

## M5 — rollback ของ optimistic update อาจ set state เป็น `null`

`hooks.js:325-345` (`saveItem`) และ `396-410` (`deleteItem`) จับ `backupRemoteItems` **ข้างใน** updater ของ `setRemoteItems`:
```js
let backupRemoteItems = null
setRemoteItems(prev => { backupRemoteItems = prev; ... })
try { await setDoc(...) } catch { setRemoteItems(backupRemoteItems) /* อาจยังเป็น null */ }
```
ถ้า write reject ก่อน React รัน updater → rollback เขียน `null` ทับ ทำให้ลิสต์ว่างและเด้งไปใช้ fallback ปกติจะรอด (`await setDoc` ให้เวลา React render) แต่ไม่มีอะไรรับประกัน

**แก้:** เก็บค่าเดิมไว้ใน ref นอก updater แล้วอ่านจาก ref ตอน rollback

## M6 — `refetchTrigger` เป็น dead code — ไม่มีทาง force refresh

`hooks.js:108-110, 130, 290` — `setRefetchTrigger` ไม่เคยถูกเรียก และ `useContentCollection` ไม่ return `refetch` ออกมา (ต่างจาก `useUserCollection`/`useUserDoc` ที่ return) → `isRefetching` เป็น `false` ตลอด คอมเมนต์ "H7: Fix cache bypass logic" จึงไม่มีผลอะไร

**แก้:** `return { items, ..., refetch: () => setRefetchTrigger(n => n + 1) }` และย้ายการอ่าน ref ออกจาก render phase (ESLint `react-hooks/refs` ฟ้อง 6 จุดในไฟล์นี้ + 6 จุดใน `settings.js`)

## M7 — `vite.config.js` ไม่ถูกใช้งานเลย

`scripts/vite-runner.mjs:8` ส่ง `configFile: false` → `npm run dev` / `build` / `preview` **ไม่อ่าน `vite.config.js`** ต้องแก้ config ที่ `vite-runner.mjs` เท่านั้น ตอนนี้สองไฟล์เนื้อหาตรงกันพอดีจึงยังไม่มีอาการ แต่เป็นกับดัก (เกี่ยวกับ H7 โดยตรง)

**แก้:** ลบ `vite.config.js` ทิ้ง หรือให้ runner โหลด (`configFile: 'vite.config.js'`) แล้ว override เฉพาะ `server`

## M8 — `/staff-members` ตกจาก `urlToPage`

`App.jsx:64-87` ไม่มี key `"staff-members"` แต่ route มีอยู่ (บรรทัด 344) → `page` กลายเป็น `"home"` ทำให้ Nav ไฮไลต์เมนูผิดเมื่ออยู่หน้านี้

**แก้:** เพิ่ม `"staff-members": "staff-members",`

## M9 — ปุ่มเล็กกว่ามาตรฐาน 44×44 (วัดจริงที่ 375px)

| ปุ่ม | ขนาด | หมายเหตุ |
|---|---|---|
| hamburger (`Open menu`) | **20 × 30** | ปุ่ม nav หลักบนมือถือ |
| theme toggle | 34 × 34 | |
| `ดูทั้งหมด →` (×3) | 62 × **18** | สูงแค่ 18px |
| `แสดงเพิ่มเติม (n ท่าน)` (×4) | 143 × 29 | หน้า /scholars |
| social icons | 36 × 36 | |

เกณฑ์ WCAG 2.5.5 / Apple HIG คือ 44×44 หนักสุดคือ hamburger 20×30 ที่ `Nav.jsx:455` ใช้ `iconButtonStyle` (`Nav.jsx:664-670`) ซึ่งไม่มี padding เลย

**แก้** — ขยาย hit area โดยไม่ต้องเปลี่ยนขนาดที่มองเห็น:
```js
const iconButtonStyle = {
  background: "transparent", border: "none", fontSize: 20, cursor: "pointer",
  color: "var(--text)",
  minWidth: 44, minHeight: 44,
  display: "grid", placeItems: "center",
  margin: -6,             // ชดเชยไม่ให้ layout ขยาย
}
```
`ดูทั้งหมด →` ให้ใส่ `padding: "10px 12px"` + `margin: "-10px -12px"`

## M10 — ลิงก์ social 5 ตัวและปุ่ม 1 ตัวไม่มีชื่อให้ screen reader

วัดได้ `noAccessibleName: 6` บนหน้า Home — icon-only `<a>` ใน footer (Facebook, YouTube, Spotify, Instagram, TikTok) และ `<button>` อีกหนึ่งตัว screen reader อ่านว่า "link" เฉย ๆ

**แก้:** ใส่ `aria-label="Facebook"` ฯลฯ ทุกตัว

## M11 — ไม่มีโครงสร้าง heading

หน้า `/scholars` มี 259 รายการใน 4 ช่วงยุค แต่ `h1: 1, h2: 0, h3: 0` — ชื่อ section เป็น `<div>` จัดสไตล์ เสีย SEO (Google ใช้ heading ทำความเข้าใจโครงสร้าง) และ screen reader ข้าม section ไม่ได้ ควรตรวจหน้าอื่นที่ใช้ `SecHeader` ด้วย

**แก้:** ให้ `SecHeader` ใน `ui/index.js` render `<h2>` และหัวข้อย่อยเป็น `<h3>`

## M12 — ปุ่มพิมพ์ crash เมื่อ popup ถูกบล็อก

`src/components/dashboard/ReflectionsPanel.jsx:332-334`
```js
const win = window.open("", "_blank")
win.document.write(`...`)          // ← win เป็น null ถ้า popup blocked → TypeError
```
**แก้:** `if (!win) { toast.error("เบราว์เซอร์บล็อกป๊อปอัป กรุณาอนุญาตแล้วลองใหม่"); return }`

## M13 — `generate-quiz` ไม่มี rate limit

`api/generate-quiz.js:224-240` — ตรวจ ID token ถูกต้องแล้ว แต่สมาชิกหนึ่งคนยิงกี่ครั้งก็ได้ (OpenAI/Anthropic, `max_tokens: 5000`) ควรใส่ quota ต่อ uid ต่อวันแบบเดียวกับที่แนะนำใน C3

หมายเหตุ: gate ใช้ `process.env.VITE_ENABLE_AI_QUIZ` — prefix `VITE_` ทำให้ตัวแปรนี้ถูก inline ลง client bundle ด้วย ถ้าใช้เป็น flag ฝั่ง server ควรตั้งชื่อไม่มี prefix (`ENABLE_AI_QUIZ` ซึ่งโค้ดรองรับอยู่แล้ว)

## M14 — `api/image-search.js` ไม่มี auth

ใช้ `PIXABAY_KEY` / `GOOGLE_API_KEY` ของคุณ, CORS `*` → ใครก็ยิงเพื่อผลาญ quota (Google CSE ฟรีแค่ 100 query/วัน) ควรใส่ `verifyIdToken` + จำกัด origin — ฟีเจอร์นี้ใช้ใน notebook ที่อยู่หลัง login อยู่แล้ว

## M15 — save ตอน unmount ไม่อัปเดต metadata

`ProNotebook.jsx:763-771` เรียกแค่ `uploadNotebookData` ไม่เขียน doc `content_notebooks/{uid}_{id}` → `updatedAt` ที่ `NotebookGalleryPanel` ใช้แสดง/เรียง "แก้ไขล่าสุด" ไม่ตรงกับเนื้อหาจริง

**แก้:** แยก metadata write เป็นฟังก์ชันแล้วเรียกจากทั้งสองเส้นทาง

## M16 — `CompressionStream` / `DecompressionStream` ไม่มี fallback

`src/utils/notebookStorage.js:5-16` — API นี้ไม่รองรับใน **Safari ต่ำกว่า 16.4** ซึ่งสำคัญมากเพราะ notebook เป็นฟีเจอร์สำหรับ iPad + stylus ผลคือ upload throw (เหลือแต่ localStorage) และ download คืน `null` → ป้อนเข้าเงื่อนไข **C2** โดยตรง

**แก้:** feature-detect แล้ว fallback เป็น JSON ธรรมดา (ตั้ง flag ที่ชื่อไฟล์หรือ contentType เพื่อให้ฝ่ายอ่านรู้):
```js
const canGzip = typeof CompressionStream !== 'undefined';
```
พร้อมแจ้งผู้ใช้ให้อัปเดต iOS ถ้าตรวจพบว่าไม่รองรับ

## M17 — `main` ตัน 1100px แต่ grid ล็อค 3 คอลัมน์

วัดที่ viewport 1920px: `main` = 1100px (`global.css:117-122`), grid = `344px 344px 344px` = 1052px → เหลือพื้นที่ว่าง ~820px และยังได้ 3 คอลัมน์เท่าเดิม

`max-width: 1100px` เหมาะกับ**เนื้อหาบทความ** (ความยาวบรรทัดที่อ่านสบาย) แต่ไม่เหมาะกับ**หน้า gallery การ์ด** — ตอนนี้ใช้ค่าเดียวกันหมด

**แนวทางที่ควรกว่า** — แยก container สองแบบ:
```css
main { width: 100%; max-width: 1100px; margin: 0 auto; padding: 36px 24px 100px; }

/* หน้าที่เป็น gallery: กว้างขึ้น + คอลัมน์ยืดตามจอ */
main.gallery { max-width: 1440px; }
.card-grid {
  display: grid;
  grid-template-columns: repeat(auto-fill, minmax(280px, 1fr));
  gap: 20px;
}
```
`auto-fill` + `minmax` จะได้ 3 คอลัมน์ที่ 1280px และ 4-5 คอลัมน์ที่ 1920px โดยไม่ต้องเขียน media query แล้วใส่ `gallery` ให้ `page` ที่เป็น `articles`/`library`/`media`/`scholars` ใน `App.jsx:294`

**ข้อดีของ layout ปัจจุบันที่ควรรักษาไว้:** ไม่พบ horizontal overflow เลยแม้แต่จุดเดียวที่ 375px ทุกหน้าที่ตรวจ (`overflowCount: 0`) — responsive พื้นฐานทำไว้ดี

---

# ⚪ LOW

1. **`FormatBtn` ถูกสร้างใน render** — `TextEditor.jsx:252` component ที่ประกาศระหว่าง render จะ unmount/remount ใหม่ทุกครั้ง เสีย state และ performance ย้ายออกไปนอก component (ESLint `react-hooks/static-components`)
2. **`countdownNotifRef` เป็น dead code** — `App.jsx:171` ถูก `.close()` แต่ไม่เคยถูก assign เป็น `null` ตลอด
3. **countdown interval สร้างใหม่ทุกครั้งที่ `readingSessions` เปลี่ยน** — `App.jsx:177-215` deps มี `readingSessions` ทำให้ล้าง/สร้าง `setInterval(…, 1000)` ซ้ำ ใช้ ref เก็บ sessions แทน
4. **`addDaysToKey` ใช้ getDate/setDate ของ timezone เบราว์เซอร์** — `streak.js:27-31` anchor วันที่เป็น `+07:00` แล้วบวกลบด้วย local `setDate` เครื่องที่อยู่โซน DST จะเพี้ยนตรงวันเปลี่ยน DST ควรคำนวณด้วย ms ตรง ๆ
5. **default `<title>` ไม่ตรงกันสองที่** — `index.html` ใช้ "…แหล่งศึกษาอิสลามแนวทางสะลัฟ" แต่ `SEOHead` reset เป็น "…แหล่งศึกษาแนวทางสะลัฟ" และ `/tracking-system` ไม่ตั้ง title เลย (ได้ default ทั้งที่ `<h1>` เป็น "Talib Club Logistics")
6. **`ADD_ATTR: ["target"]` ไม่บังคับ `rel="noopener"`** — `ArticleDetail.jsx:446-460` ลิงก์ `target="_blank"` ในเนื้อบทความเปิดช่องให้ reverse tabnabbing ควร post-process เพิ่ม `rel="noopener noreferrer"` ให้ทุก `target="_blank"`
7. **`parseBody(req)` ถูกเรียกซ้ำ** — `api/reserve-book-campaign.js:47-48` เรียกสองครั้งเพื่ออ่านสองฟิลด์
8. **`counters` ต้องเป็น staff** — `firestore.rules:150-152` ปัจจุบันทุกเส้นทางฝั่งสมาชิกส่ง `id` มาเองครบ จึงยังไม่มีอาการ แต่ถ้าวันหลังมี `saveItem()` จากหน้าสมาชิกที่ไม่ส่ง `id` → `getNextSequenceId` จะ transaction บน `counters` แล้วโดน PERMISSION_DENIED ควรใส่คอมเมนต์เตือนไว้ที่ `hooks.js:422`
9. **ESLint: 120 errors + 193 warnings** — ในนั้นเป็นของจริง ~30 รายการ ที่เหลือคือ `no-unused-vars` 165 ตัว และ `no-empty` (catch ว่าง) 24 ตัว ส่วน `no-undef` ของ browser globals (`MediaRecorder`, `Audio`, `indexedDB`, `caches`, `Path2D`, `CompressionStream`, `Response`) เป็นแค่ config ไม่ครบ — เพิ่มใน `browserGlobals` ที่ `eslint.config.js:5-37` เพื่อให้ error ที่เหลือเป็นของจริงทั้งหมด
10. **`process.env.NODE_ENV` ปลอดภัย** — ที่ `ErrorBoundary.jsx:58`, `MemberDashboard.jsx:95,148` ESLint ฟ้อง `no-undef` แต่ Vite แทนค่าให้ตอน build (ยืนยันแล้วว่า `dist/` ไม่มี `process.env` เหลือ) ไม่ต้องแก้โค้ด แก้แค่ config ตามข้อ 9

---

## สิ่งที่ทำไว้ดีแล้ว

เพื่อความเป็นธรรม — หลายส่วนของโปรเจกต์นี้ทำไว้แข็งแรง:

- **XSS ปิดครบ** — `dangerouslySetInnerHTML` ทุกจุดที่รับข้อมูลจาก user/remote ผ่าน `DOMPurify.sanitize()` และ `api/seo-prerender.js` escape ทุก interpolation
- **API ส่วนใหญ่ auth ถูกต้อง** — `send-push`, `send-telegram`, `abuiyaad-scrape`, `abuiyaad-translate` ตรวจ ID token + role staff ผ่าน firebase-admin ครบ
- **ระบบจอง book campaign เขียนดีมาก** — `reserve-book-campaign` / `submit-book-registration` ใช้ Firestore transaction คุมโควตา ตรวจ hold หมดอายุ ป้องกัน race condition ได้จริง
- **Responsive พื้นฐานแน่น** — ไม่มี horizontal overflow เลยทุกหน้าที่ตรวจที่ 375px
- **Firestore rules ฝั่ง user data แน่นหนา** — pattern `ownsExistingDocument()` + `keepsOwnership()` ป้องกันการแย่ง doc ของคนอื่นได้ถูกต้อง และกันการยกระดับ `role` ตัวเองไว้แล้ว
- **Optimistic update + rollback, request dedup, 3-ชั้น cache** ใน contentStore เป็นดีไซน์ที่คิดมาดี (แค่มีรอยรั่วตาม M3–M6)
- **`build` ผ่านไม่มี error** และมี sitemap generator อัตโนมัติ

---

## ลำดับการลงมือที่แนะนำ

| ลำดับ | รายการ | เหตุผล | แรง |
|---|---|---|---|
| 1 | **C1** storage.rules | ข้อมูลรั่วอยู่ตอนนี้ แก้ไฟล์เดียว deploy ทันที | 30 นาที |
| 2 | **C3** api/ai.js | บิลบานได้ทุกวินาที ลอกโครงจาก send-telegram.js | 1 ชม. |
| 3 | **C2** notebook data loss | ทุกครั้งที่เน็ตกระตุกคือความเสี่ยงงานหาย | 2 ชม. |
| 4 | **H3 + H4** ปุ่ม throw | ตัวละ 3 บรรทัด ผู้ใช้เจอทุกวัน | 15 นาที |
| 5 | **C4** proxy-pdf + **H1/H2** rules | ปิดช่อง SSRF และ PII | 2 ชม. |
| 6 | **H5** autosave + **M1** recovery | คู่กันทั้งสองข้อเพื่อกันข้อมูลหาย | 2 ชม. |
| 7 | **H6** ปลด auth gate | ผลต่อ UX/SEO ของทุกหน้าสาธารณะ | 1 ชม. |
| 8 | **H8 + M9 + M10** a11y | ทำที่ `Card`/`ArticleCard`/`iconButtonStyle` ครอบทั้งเว็บ | 3 ชม. |
| 9 | **M17 + H7** layout + bundle | คุณภาพชีวิตผู้ใช้ | 3 ชม. |
| 10 | ที่เหลือ | | ตามสะดวก |
