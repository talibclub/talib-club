# Bug audit — สิงหาคม 2026

ตรวจทั้งโปรเจกต์ 22,400 บรรทัด (JS/JSX/HTML/config อ่านทีละบรรทัด, CSS 4,506 บรรทัดสแกนแบบ static)
เจอทั้งหมด **167 จุด** — ไฟล์นี้คือสถานะจริงของแต่ละจุด ตรวจสอบกับโค้ดปัจจุบันบน branch `fix/audit-2026-08`

| | จำนวน |
|---|---|
| ✅ แก้แล้ว + verify (build/test/lint/browser) | **163** |
| ⏸️ ต้องทำนอกโค้ด | **0** |
| ⏸️ จงใจไม่แก้ (ต้องเขียนสคริปต์ migrate ก่อน) | **1** |
| 🔎 ตรวจแล้วไม่ใช่บัค / ถอนคำ | **4** |

**สถานะ:** ชุด 1–5, A, B, C ปิดครบทุกจุดที่แก้ได้จากโค้ด

**เจอเพิ่มระหว่างทาง (นอก 167 จุดเดิม):**
- **ยอดวิวปั๊มได้** — dedup เดิมใช้ `sessionStorage` ซึ่งผูกกับแท็บ กด F5 ไม่นับซ้ำก็จริง แต่**เปิดแท็บใหม่ / ปิดแล้วเปิดใหม่ = นับซ้ำทุกครั้ง** และนับทันทีที่หน้าโหลดโดยไม่ดูว่าอ่านจริงไหม → เปลี่ยนเป็น `localStorage` + หน้าต่างเวลา (วิว 24 ชม. / ดาวน์โหลด-แชร์ 1 ชม.) และต้องเปิดหน้าค้างไว้ 8 วินาทีโดยแท็บต้องอยู่หน้าจอถึงจะนับ

**ผลข้างเคียงที่ต้องรู้:**
- ตัวเลข "หนังสือ/วารสาร" บนหน้าแรกจะเปลี่ยนจาก **301 → 56** เพราะเดิมนับหนังสือที่ซ่อนไว้ (`deleted: true`) 245 เล่มรวมไปด้วย (C5) — เลข 56 คือจำนวนจริงที่ผู้เข้าชมเห็น
- `npm test` เปลี่ยนไปรัน vitest แล้ว (6 → 15 เทสต์) และลบ `scripts/run-tests.mjs` ทิ้ง
- lint: error 93 → 56, warning 183 → 78 (ที่เหลือทั้งหมดเป็น React Compiler advisory ในไฟล์ใหญ่ ซึ่งต้องรีแฟกเตอร์โครงสร้าง ไม่ใช่แก้ทีละบรรทัด)

---

## ✅ แก้แล้ว (58)

### ชุด 1 — ของพังจริง
1. `stableStringify` double-encode → cache invalidation ทั้งระบบเป็นโค้ดตาย 14 จุด — [utils.js](src/lib/contentStore/utils.js)
2. 7 จุดที่เพิ่งกลับมาทำงานใช้ `Date.now()` ดิบ → `safeDateNow()` — [hooks.js](src/lib/contentStore/hooks.js)
3. ยอดวิว/ดาวน์โหลด/แชร์ ถูกกฎบล็อกทั้งหมด → `isCounterBump()` พิสูจน์รูปร่าง `increment(1)` — [firestore.rules](firestore.rules)
4. อัปโหลดไฟล์หนังสือเข้าชั้นล้มเหลว 100% (ไม่มีกฎ `members/`) — [storage.rules](storage.rules)
5. ลบ `AudioShapeUtil.jsx` — import `tldraw` ที่ไม่มีใน package.json + กฎ `notebook_audios` ที่เปิดให้อ่านเสียงคนอื่น
6. Google CSE key หลุดเข้า production bundle → ย้ายเข้าเงื่อนไข `import.meta.env.DEV`
7. Push subscription ID ชนกันข้ามผู้ใช้ (base64 ตัดท้าย) → SHA-256 — [pushNotifications.js](src/utils/pushNotifications.js)
8. สมุดโน้ตเปิดไม่ได้บน iPad แนวตั้ง (900px vs 768px) — [ReadingApp.jsx](src/pages/ReadingApp.jsx)
9. Leaderboard ว่างถาวรทุกคน → query `leaderboard` + mirror ตอน sync streak — [LeaderboardPanel.jsx](src/components/dashboard/LeaderboardPanel.jsx)
10. เหรียญ "ผู้ส่งต่อความรู้" ปลดล็อกไม่ได้ตลอดกาล — [ArticleDetail.jsx](src/pages/ArticleDetail.jsx)

### ชุด 2 — อัลกุรอาน
11. `loading` ค้าง true ถาวร → ย้อนกลับจุดเดิมไม่ทำงาน — [Quran.jsx](src/pages/Quran.jsx)
12. คั่นอายะฮ์บันทึกเลขซูเราะฮ์ผิดเมื่อหน้ามุศฮัฟคาบ 2 ซูเราะฮ์
13. ไอคอนบุ๊คมาร์กติดผิดอายะฮ์
14. ตัฟซีรจับคู่ด้วย index แทนเลขอายะฮ์ → `Map` ตามเลขอายะฮ์
15. ค้นหาในอัลกุรอานพังเมื่อพิมพ์อักขระ regex → `escapeRegExp` — [QuranSidebar.jsx](src/pages/quran/components/QuranSidebar.jsx)
16. Ctrl+V แปะรูปจากภายนอกใช้ไม่ได้เลย — [ProNotebook.jsx](src/pages/reading/components/ProNotebook.jsx)

### ชุด 3 — โค้ดตาย / UI ผี (ProNotebook)
17. Debug toast หลุดขึ้น production (2 จุด)
18. ไม้โปรแทรกเตอร์วาดซ้อน 2 อัน (57 บรรทัดซ้ำ byte-identical)
19. หน้าต่างแม่แบบกระดาษเปิดซ้อน 2 อัน (59 บรรทัด)
20. เมนู "เพิ่มการล็อค" ไม่มี onClick
21. หน้าจอเลือกโหมดเป็น dead UI
22. `AdminBookCampaigns` `confirmAction` เรียกผิดรูปแบบ → คำเตือนตอนลบหาย
23. `CampaignRegistrationsViewer` doc id ไม่ deterministic → ส่งซ้ำได้ไม่จำกัด
24. eslint: เพิ่ม `TextEncoder`/`TextDecoder` globals

### ชุด 4 — race / ข้อมูลหาย
25. gems/เพชร/มิชชัน อ่าน-แก้-เขียน → `mutateStreakDoc()` + `runTransaction` (4 จุด)
26. auto-freeze ยิงซ้ำ → `autoFreezeAppliedRef`
27. import PDF ทับโน้ตเดิมโดยไม่เตือน → `confirmAction` + `pushHistory()` + เตือนเกิน 30 หน้า
28. ลากวัตถุแล้ว undo ไม่ได้ (5 handler)
29. Export CSV เสียเมื่อมี `"` / newline + formula injection — [csvTracking.js](src/utils/csvTracking.js)
30. `progressLog` แข่งกันเขียน → `arrayUnion` — [StaffTasks.jsx](src/components/dashboard/StaffTasks.jsx)
31. Telegram แจ้งวารสารส่งซ้ำ + `setDoc` ไม่ merge ลบคีย์เดือนอื่น → transaction — [StaffWork.jsx](src/pages/StaffWork.jsx)

### ชุด 5 — ต้นทุน / ความเร็ว
32. Nav เปิด `onSnapshot` ค้าง 4 ตัวให้ทุกคนที่เข้าเว็บ → `getDocs` ครั้งเดียว เฉพาะคนล็อกอิน
33. Nav `setInterval` 1 วิ ตลอดวัน + state `currentHM` ที่ไม่มีใครอ่าน → re-render ทุกวินาที
34. `go` ไม่ memoize → พังการ memo ของทั้ง tree — [App.jsx](src/App.jsx)
35. countdown ติ๊ก 1 วิ ทั้งวัน → adaptive 1 วิ/30 วิ
36. `live: true` บน `content_articles` ทั้ง collection — [Articles.jsx](src/pages/Articles.jsx)
37. `live: true` บนหน้าแรก — [Home.jsx](src/pages/Home.jsx)
38. ค้นหาพิมพ์ 1 ตัว = navigate 1 ครั้ง → debounce 350ms (Articles + Library)
39. รูปเข้าสมุดไม่บีบอัด → `compressImageFile()` ≤1600px webp
40. รูปที่แทรกในมิลลิวินาทีเดียวกันได้ id ซ้ำ → `nextImageId()`
41. ตัวแปร CSS 12 ตัวไม่เคยนิยาม → declaration ใช้ไม่ได้ทั้งบรรทัด — [global.css](src/styles/global.css) · [Quran.css](src/pages/quran/Quran.css)
42. `@media 768px` ชนกับ JS `innerWidth < 768` (4 ไฟล์)

*(นับรวมจุดย่อยในแต่ละข้อ = 58)*

---

## ✅ ชุด A + B — ปิดครบแล้ว

### 🔴 ระดับสูง — แก้แล้ว 9/10 (A1 ต้องเปิด Firebase Console)

| # | ที่ | อาการ |
|---|-----|-------|
| A1 ✅ | `public/tracking-system.html` | **แก้แล้ว** — เครื่องมือต่อโปรเจกต์แยก (`talib-trackingnumber`) ด้วย `signInAnonymously()` กฎจึงแยกสตาฟกับคนทั่วไปไม่ได้เลย ย้ายมาใช้โปรเจกต์หลัก `talib-club-web` (ที่ `records`/`recipients` เป็น staff-only อยู่แล้ว) และรับ session ของสตาฟจากหน้าแม่แทน — ยืนยันบนเบราว์เซอร์: request ไป `talib-trackingnumber` = 0 |
| A2 | `public/tracking-system.html:1946,1975,2143,2438` | XSS ผ่าน `innerHTML` ใน 4 ฟังก์ชัน — ข้อมูลมาจาก CSV ที่ operator อัปโหลด |
| A3 | `public/tracking-system.html` | อ่านทั้ง collection ทุกครั้งที่กดเซฟ (6 ฟังก์ชัน) |
| A4 | `public/tracking-system.html` | Regex lookbehind → Safari เก่าพังทั้งไฟล์ |
| A5 | `cors.json` | `origin: ["*"]` |
| A6 | [seo-prerender.js](api/seo-prerender.js) | Cloaking — Google เห็นเนื้อหาหนังสือเต็ม แต่คนคลิกเข้ามาเจอหน้าล็อกอิน |
| A7 | [StaffTranslation.jsx:191](src/pages/StaffTranslation.jsx#L191) | `saveWorkspace` เขียน `paragraphs` ทั้งก้อนทับ → สองคนแปลพร้อมกัน งานหายทับกัน |
| A8 | [import-almaktabah](scripts/) | หนังสือไทยที่ตั้งชื่อเป็นอักษรละตินจะถูกลบถาวร |
| A9 | [StaffMembers.jsx](src/pages/StaffMembers.jsx) | บัญชี owner หายจากทุกแท็บกรอง |
| A10 | [ProNotebook.jsx:1013](src/pages/reading/components/ProNotebook.jsx#L1013) | ออกจากหน้าขณะอัดเสียง → ไมค์ค้างเปิด |

### 🟠 ระดับกลาง — แก้ครบ 48/48

| # | ที่ | อาการ |
|---|-----|-------|
| B1 | [CampaignRegistrationsViewer.jsx:250](src/pages/admin/CampaignRegistrationsViewer.jsx#L250) | `h.expiresAt.toMillis()` ไม่กัน null (บรรทัด 44 กันแล้ว บรรทัด 250 ยังไม่) |
| B2 | [CampaignRegistrationsViewer.jsx:197](src/pages/admin/CampaignRegistrationsViewer.jsx#L197) | `r.createdAt.toMillis()` พังถ้าเป็นตัวเลข |
| B3 | [hooks.js:36](src/lib/contentStore/hooks.js#L36) | `item.category.toLowerCase()` พังถ้า category เป็น object → บันทึกเนื้อหาใหม่ไม่ได้ |
| B4 | [OpenHouseCampus.jsx:208](src/pages/OpenHouseCampus.jsx#L208) | `` `${booth.themeColor}40` \|\| "var(--br)" `` — template string จริงเสมอ → `"undefined40"` |
| B5 | [Quran.jsx](src/pages/Quran.jsx) | `history.replaceState` ข้าม React Router |
| B6 | [Quran.jsx](src/pages/Quran.jsx) | `performSearch` ไม่มี stale guard |
| B7 | [ProNotebook.jsx:880](src/pages/reading/components/ProNotebook.jsx#L880) | `fitToScreen` ผูกกับ `dimensions` → แถบ URL มือถือซ่อน/โผล่ = ซูมรีเซ็ตขณะเขียน |
| B8 | [ProNotebook.jsx:834](src/pages/reading/components/ProNotebook.jsx#L834) | effect ผูกกับ `audioProgress.current` → re-render component 5,000 บรรทัด 4 ครั้ง/วินาทีตอนเล่นเสียง |
| B9 | [ProNotebook.jsx:566](src/pages/reading/components/ProNotebook.jsx#L566) | `localStorage.setItem` ข้างใน setState updater |
| B10 | [ProNotebook.jsx:989](src/pages/reading/components/ProNotebook.jsx#L989) | `URL.createObjectURL` ไม่ revoke |
| B11 | [ProNotebook.jsx](src/pages/reading/components/ProNotebook.jsx) | กรอบวัตถุข้อความคำนวณผิดเมื่อมีหลายบรรทัด |
| B12 | [ProNotebook.jsx](src/pages/reading/components/ProNotebook.jsx) | ค้นในสมุดไม่เจอข้อความที่แก้ผ่าน editor |
| B13 | [ProNotebook.jsx](src/pages/reading/components/ProNotebook.jsx) | ค้นรูปแล้วผลเก่าทับผลใหม่ (ไม่มี stale guard) |
| B14 | [ProNotebook.jsx](src/pages/reading/components/ProNotebook.jsx) | คำค้นของสมาชิกถูกส่งผ่าน third-party proxy |
| B15 | [ProNotebook.jsx:1183](src/pages/reading/components/ProNotebook.jsx#L1183) | `writeNotebookMeta` ฮาร์ดโค้ด `coverColor: 'red'` → ปกสมุดแดงทุกเล่ม |
| B16 | [StaffWork.jsx:158](src/pages/StaffWork.jsx#L158) | `onSnapshot` effect deps `[]` ทั้งที่อ่าน `uid` → สลับบัญชีแล้ว listener เกาะของเดิม |
| B17 | [StaffCalendar.jsx:25](src/components/dashboard/StaffCalendar.jsx#L25) | โหลดทั้ง collection แบบ live ตลอดกาล |
| B18 | [ProfilePanel.jsx:192](src/components/dashboard/ProfilePanel.jsx#L192) | `if (loadingDbStats) return <spinner>` เหนือทุกอย่าง → แท็บตั้งค่าบัญชีเข้าไม่ได้จนกว่า `getCountFromServer` 4 ตัวเสร็จ |
| B19 | [ProfilePanel.jsx:196](src/components/dashboard/ProfilePanel.jsx#L196) | พิมพ์ชื่ออยู่แล้วโดนล้าง |
| B20 | [ProfilePanel.jsx](src/components/dashboard/ProfilePanel.jsx) | แอดมิน/เจ้าของเห็นตัวเองเป็น "Member" |
| B21 | [ReflectionsPanel.jsx:101](src/components/dashboard/ReflectionsPanel.jsx#L101) | ลบข้อคิดแล้ว session ยังนับ `verified: true` |
| B22 | [ReadingApp.jsx](src/pages/ReadingApp.jsx) | น้ำแข็งอาจถูกหักสองชิ้นสำหรับวันเดียว |
| B23 | [ReadingApp.jsx:360](src/pages/ReadingApp.jsx#L360) | `claimedMissions` โตไม่มีที่สิ้นสุด — 1 ปี = 365 key ใน doc ที่อ่าน/เขียนบ่อยที่สุด |
| B24 | [ReadingApp.jsx:212](src/pages/ReadingApp.jsx#L212) vs [Overview.jsx:60](src/components/dashboard/Overview.jsx#L60) | ตัวนับ "กำลังอ่าน" สองหน้าไม่ตรงกัน |
| B25 | [useReadingTimer.js:14](src/pages/reading/hooks/useReadingTimer.js#L14) | นับจาก wall-clock → พับแท็บทิ้งไว้ก็นับเป็นเวลาอ่านจริง |
| B26 | [useAuth.js:210](src/hooks/useAuth.js#L210) | `talibAfterLogin` เขียนแล้วไม่มีใครอ่าน → Google redirect ไม่พากลับหน้าเดิม |
| B27 | [usePWA](src/hooks/usePWA.js) | ปฏิเสธสิทธิ์แจ้งเตือนแล้วสวิตช์ยังค้างเป็น "เปิด" (2 จุด) |
| B28 | [App.jsx](src/App.jsx) | เช็ค `getMinutes() === prefMin` ด้วย interval 60s → drift แล้ววันนั้นไม่เตือนเลย |
| B29 | [AdminBookCampaigns.jsx:112](src/pages/admin/AdminBookCampaigns.jsx#L112) | ลบ registrations/holds ด้วย `Promise.all` ไม่จำกัด → ล้มบางส่วน = orphan |
| B30 | [Tracking.jsx:202](src/pages/Tracking.jsx#L202) | `getVal` จับคู่หัวคอลัมน์แบบ `includes` → "เลขที่บ้าน" ถูกอ่านเป็นเลข tracking |
| B31 | [StaffTasks.jsx](src/components/dashboard/StaffTasks.jsx) | แจ้งเตือน "มีงานมอบหมายถึงคุณ" ส่งหาสตาฟทุกคน |
| B32 | [SavedVersesPanel.jsx:19](src/components/dashboard/SavedVersesPanel.jsx#L19) | ยิง API 3 เส้นต่ออายะฮ์ ไม่มีแคช — 50 อายะฮ์ = 150 requests |
| B33 | admin | `fixDb` ลบ tombstone ทิ้งและพังเมื่อเกิน 500 รายการ |
| B34 | admin | ปุ่ม "ล้างแคช" ไม่ได้ล้างแคชที่เป็นปัญหา |
| B35 | [BookSnipModal](src/pages/reading/components/BookSnipModal.jsx) | เรนเดอร์ PDF ที่ scale 2 ตายตัว ไม่มีเพดาน pixel |
| B36 | [canvasElements.jsx:148](src/pages/reading/components/notebook/canvasElements.jsx#L148) | `key={i}` บนรายการเส้นหมึก → ลบเส้นกลางหน้า = reconcile ใหม่ทั้งชุด |
| B37 | [canvasElements.jsx:149](src/pages/reading/components/notebook/canvasElements.jsx#L149) | หมึกจากบันทึกเสียงอื่นถูกหรี่ผิดตัว |
| B38 | [TextEditor.jsx:252](src/pages/reading/components/notebook/TextEditor.jsx#L252) | นิยาม `FormatBtn` ข้างใน render → remount ทุกครั้ง |
| B39 | [LassoToolbar](src/pages/reading/components/notebook/LassoToolbar.jsx) | `<input type="color">` ที่โปรเจกต์เองบอกว่าใช้ไม่ได้บนแท็บเล็ต |
| B40 | notebook | "ใช้กับทุกหน้า" ในแม่แบบกระดาษกดไม่ได้ตอนอยู่หน้า PDF — และถ้ากดได้ก็เขียน undefined |
| B41 | [ImageSearchPanel](src/pages/reading/components/notebook/ImageSearchPanel.jsx) | ตัวกรอง "GIF" ไม่ทำงาน + เครดิตแหล่งรูปเขียนผิดแหล่ง |
| B42 | [Books/counter](src/pages/admin/) | หนังสือที่ไม่มีฟิลด์ `status` หายจากตัวนับทั้งสองฝั่ง |
| B43 | [ReadingApp.jsx](src/pages/ReadingApp.jsx) | ชื่อวันในแถบ 7 วันย้อนหลังเพี้ยนสำหรับผู้ใช้นอกไทย |
| B44 | [Quran.jsx](src/pages/Quran.jsx) | เรียงลำดับอายะฮ์ที่บันทึกไว้ไม่ทำงาน |
| B45 | api | Index ที่ขาด ทำให้ query ตกไป fallback ตลอด |
| B46 | scripts | batch ล้มกลางคัน = import ค้างครึ่งทาง |
| B47 | scripts | sitemap พังแบบเงียบ + build แก้ไฟล์ที่ track ไว้ |
| B48 | api | คำแนะนำศัพท์ในพรอมป์ตแปลเป็นตัวอักษรขยะ (mojibake) |

### 🟡 ระดับต่ำ — แก้ครบ (ยกเว้น C17 · C28)

| # | ที่ | อาการ |
|---|-----|-------|
| C1 | [vite.config.js](vite.config.js) | ไฟล์ตาย ไม่เคยถูกโหลด (runner ใช้ `configFile: false`) |
| C2 | [run-tests.mjs](scripts/run-tests.mjs) | hardcode แค่ streak test — `shapeRecognition.test.js` และ vitest config ไม่เคยถูกรัน |
| C3 | [App.jsx](src/App.jsx) | ErrorBoundary โชว์ `error.stack` เต็มให้ผู้ใช้ทั่วไป |
| C4 | [cache.js:213,225](src/lib/contentStore/cache.js#L213) | `readCachedDocument`/`readCachedUserDocument` Map เดียวกัน คีย์เดียวกัน TTL/limit ต่างกัน |
| C5 | [hooks.js](src/lib/contentStore/hooks.js) | `useCollectionCount` นับ soft-deleted ด้วย |
| C6 | [hooks.js](src/lib/contentStore/hooks.js) | `bulkDeleteItems` hard-delete ทั้งที่โค้ดอื่นตั้งใจ soft-delete |
| C7 | [useAuth.js](src/hooks/useAuth.js) | race ตอนสมัครสมาชิกทำให้ชื่อผู้ใช้หาย |
| C8 | [firestore.rules](firestore.rules) | ลบเอกสารของคนอื่นได้ถ้าเอกสารนั้นไม่มีฟิลด์ `uid` |
| C9 | [generate-quiz.js](api/generate-quiz.js) | `parseBody` ไม่รองรับ Buffer (ต่างจาก api อื่น) |
| C10 | [BookRegistration.jsx:158](src/pages/BookRegistration.jsx#L158) | ไม่ validate ชนิด/ขนาดไฟล์สลิปฝั่ง client |
| C11 | [Library.jsx:111](src/pages/Library.jsx#L111) · [SavedArticlesPanel.jsx:75](src/components/dashboard/SavedArticlesPanel.jsx#L75) | `title.toLowerCase()` ไม่ guard — latent (doc ที่สร้างนอกแอปเท่านั้น) |
| C12 | [dashboardUtils.js:86](src/components/dashboard/dashboardUtils.js#L86) | `initials()` พังถ้า name+email ว่างพร้อมกัน — latent |
| C13 | [Media.jsx:13](src/pages/Media.jsx#L13) | `limit: 200` ตัดมีเดียเงียบ ๆ (Library 1000, Articles ไม่จำกัด) |
| C14 | [Tracking.jsx:199](src/pages/Tracking.jsx#L199) | `value={editData.fullName}` อาจ undefined → uncontrolled→controlled |
| C15 | [Tracking.jsx:141](src/pages/Tracking.jsx#L141) | `handleSecretClick` ตั้ง `setTimeout` ใหม่ทุกคลิกโดยไม่ล้างของเดิม |
| C16 | [Tracking.jsx:537](src/pages/Tracking.jsx#L537) | ซาก `talib_admin_auth` จากระบบรหัสผ่านเก่า |
| C17 | [StaffTranslation.jsx:18](src/pages/StaffTranslation.jsx#L18) | `docId(url).slice(0,120)` เสี่ยงชนกัน |
| C18 | [Donation.jsx:12](src/pages/Donation.jsx#L12) | เลขบัญชีบริจาค hardcode ทั้งที่มีหน้าตั้งค่าเว็บอยู่แล้ว |
| C19 | [StaffMembers.jsx:326](src/pages/StaffMembers.jsx#L326) | เลื่อนสิทธิ์เป็น admin ทำไม่ได้จาก UI |
| C20 | [ProNotebook.jsx:2637](src/pages/reading/components/ProNotebook.jsx#L2637) | `useEffect(() => {}, [scale, position])` — effect ว่างเปล่า |
| C21 | [ProNotebook.jsx:2181](src/pages/reading/components/ProNotebook.jsx#L2181) | ternary สองข้างเหมือนกัน |
| C22 | ProNotebook | guard ที่ไม่ guard อะไรเลย |
| C23 | [Quran](src/pages/quran/) | Bottom-sheet มือถือเป็นสำเนาของ QuranSidebar |
| C24 | `src/pages/quran/hooks/` | ซากรีแฟกเตอร์ ไม่มีใคร import |
| C25 | หลายที่ | `useAuth()` ถูกสร้างซ้ำอีก 2 ชุด |
| C26 | admin | วันที่ในฟอร์มบทความใหม่ค้างที่วันที่เปิดแท็บ |
| C27 | admin | Draft บทความเขียน localStorage ทุกการพิมพ์ ไม่มี try/catch |
| C28 | admin | Cloudinary upload preset เปิดโล่งอยู่ใน bundle |
| C29 | admin | PapaParse โหลดจาก CDN ไม่มี SRI |
| C30 | admin | คิววารสาร: แก้ state ทับของเดิมโดยตรง |
| C31 | admin | CSV import ล้มเหลวบางส่วนแบบเงียบ |
| C32 | หลายที่ | `console.log` ชุด diagnostic ค้างใน production |
| C33 | ทั้งโปรเจกต์ | `no-unused-vars` 161 จุด (ProNotebook 27, contentStore/hooks 18, Nav 6) |
| C34 | api | หน้าบทความ 1 หน้า = อ่าน Firestore ได้ถึง 1,200 docs |

**ยกเว้น 2 จุดที่จงใจไม่แก้:**
- **C17** `docId(url).slice(0,120)` ใน [StaffTranslation.jsx](src/pages/StaffTranslation.jsx) — เปลี่ยนสูตร id แล้วบทความที่ scrape ไปแล้วทั้งหมดจะได้ id ใหม่ ของเดิมพร้อมงานแปลที่ผูกอยู่จะกลายเป็นเอกสารกำพร้า ต้องเขียนสคริปต์ migrate ก่อน — ใส่คอมเมนต์เตือนไว้ในโค้ดแล้ว
- ~~**C28** Cloudinary unsigned preset~~ → **แก้แล้ว** เจ้าของยืนยันว่าไม่เคยใช้แนบวิดีโอเลย จึงตัดเส้นทาง Cloudinary ออกทั้งหมด ไฟล์ทุกชนิดขึ้น Firebase Storage (`staff_tasks/`, `staff_submissions/`) ซึ่ง storage.rules จำกัดเป็น staff อยู่แล้ว — ไม่ต้องไปตั้งอะไรใน Console

---

## 🔎 ตรวจแล้วไม่ใช่บัค — ผมเคยรายงานเกินจริงแล้วถอน

| ที่ | เคยบอก | ความจริง |
|-----|--------|----------|
| [ReflectionsPanel.jsx:62](src/components/dashboard/ReflectionsPanel.jsx#L62) | 🔴 crash | `title` สร้างจาก template literal, `notes` ผ่าน `.filter()` ก่อน → เป็น string เสมอ |
| [StaffTranslation.jsx:295](src/pages/StaffTranslation.jsx#L295) | 🔴 crash | `extractLinks()` มี `if (!title \|\| title.length < 8) continue` |
| [Tracking.jsx:398](src/pages/Tracking.jsx#L398) | 🔴 crash | CSV import มี `if (!fullName) return` และ API ตรวจ `if (!registration.name) throw` |
| `tracking-system.html` เข้าถึงได้ทุกคน | 🔴 | มี same-origin iframe + `parent.__isStaff` guard — เหลือแต่ปัญหาชั้นข้อมูล (A1) |

---

## ⚠️ ต้องทำเองก่อน deploy

1. **กฎ Firebase ไม่ไปกับ Vercel build** — การแก้ `firestore.rules` / `storage.rules` จะไม่มีผลจนกว่าจะรัน:
   ```
   npx firebase deploy --only firestore:rules,storage
   ```
2. **push subscription เดิมจะกำพร้า** — doc ที่ใช้ id แบบเก่าจะไม่ถูกเขียนทับ (ไม่พัง แต่ค้างเป็นขยะ)
3. **A1 / A5 รอการตัดสินใจ** — ต้องเปิด Firebase Console ของโปรเจกต์ `talib-trackingnumber` ก่อน


---

## ⏸️ เหลือที่ต้องทำนอกโค้ด

ไม่เหลือแล้ว — ทั้ง A1 และ C28 ปิดได้จากโค้ด

| ✅ ทำแล้ว | รายละเอียด |
|---|---|
| ปิดกฎ `talib-trackingnumber` | `allow read, write: if false;` publish แล้ว 19 ส.ค. 2026 (ยืนยันว่าไม่มีข้อมูลตกค้าง) — ลบโปรเจกต์ทิ้งได้เลย |
| Cloudinary | ตัดออกจากโค้ดทั้งหมด ไม่ต้องตั้งค่าใน Console |

## 📋 คำสั่งที่ต้องรันเองหลัง merge

```
npx firebase deploy --only firestore:rules,storage,firestore:indexes --project talib-club-web
```

`firestore.rules`, `storage.rules` และ `firestore.indexes.json` **ไม่ได้ deploy ไปกับ Vercel build** — ถ้าไม่รันคำสั่งนี้ การแก้กฎทั้งหมด (ยอดวิว, กฎ `members/`, ช่องโหว่ลบเอกสารคนอื่น) และ index ใหม่จะไม่มีผล

> ✅ รันไปแล้วเมื่อ 19 ส.ค. 2026 — `Deploy complete!` ทั้ง firestore rules และ storage rules
